<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.project.percobaanku.tokoku">

    <application
        android:allowBackup="true"
        android:icon="@color/hijau_tokoku"
        android:label="@string/app_name"
        android:roundIcon="@drawable/tokoku"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".DetailBarang" />
        <activity android:name=".CardViewBarang" />
        <activity android:name=".AboutMe" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>